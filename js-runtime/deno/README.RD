# Deno: Java based latest Javascript engine, it also runs Typescript code

## How to use Deno to run Javascript/Typescript Codes
* Install Deno
* Set path under the system environment variable
* run the command `deno run jsfile.js` or `deno run tsfile.ts` on the terminal